import assert from "power-assert";
import Tween from "../src/Tween";

describe('Tween', function() {
    var setting;
    beforeEach(() => {
        setting = {

        };
    });

    it('play', () => {
        console.log(Tween)
        assert(0 === 0);
        assert(3 === 3);
    });
});